# ====================================
# FRONTEND ENVIRONMENT CONFIGURATION
# ====================================
# ⚠️  CRITICAL SECURITY NOTE:
# This file is for frontend-only, NON-SENSITIVE configuration.
# API keys should NEVER be placed in this file!
# All API keys MUST go in backend/.env (see backend/.env.example)
#
# WHY?
# - Frontend env vars (VITE_*) are embedded into JavaScript at build time
# - Anyone can inspect the built JavaScript and extract these values
# - Using API keys here is a SECURITY VULNERABILITY
#
# CORRECT ARCHITECTURE:
# - Frontend: Uses relative URLs like /api/llm, /api/tts
# - Backend: Reads API keys from backend/.env and proxies requests
# ====================================

# Environment
NODE_ENV=

# Backend URL (only needed for development when frontend/backend run separately)
# In production, leave this EMPTY - the app uses relative URLs that work on any domain
# Example for dev: http://localhost:3001
VITE_BACKEND_URL=
# ====================================
# OPTIONAL: Model/Voice Overrides
# ====================================
# These are NOT sensitive and can be configured here if needed
# These control which models/voices the UI offers, not authentication

# OpenAI Model Selection
# VITE_OPENAI_MODEL=gpt-5-nano

# ElevenLabs Configuration
# VITE_ELEVENLABS_MODEL=eleven_turbo_v2_5
# VITE_ELEVENLABS_VOICE_ID=xsGHrtxT5AdDzYXTQT0d

# Gemini LLM Configuration
# VITE_GEMINI_LLM_MODEL=gemini-2.5-flash-lite

# Gemini TTS Configuration
# VITE_GEMINI_TTS_MODEL=gemini-2.5-flash-preview-tts
# VITE_GEMINI_TTS_VOICE=Zephyr

# ====================================
# ⚠️  WHAT NOT TO PUT HERE:
# ====================================
# ❌ VITE_OPENAI_API_KEY=xxx        (NEVER!)
# ❌ VITE_ELEVENLABS_API_KEY=xxx    (NEVER!)
# ❌ VITE_GEMINI_API_KEY=xxx        (NEVER!)
# ❌ Any other API keys or secrets  (NEVER!)
#
# ✅ Instead: Put API keys in backend/.env
# ====================================

# Database (backend only, included here for documentation)
DATABASE_PATH=./database/stories.db
DATABASE_BACKUP_INTERVAL=3600000

# Logging
LOG_LEVEL=warn
LOG_FILE_MAX_SIZE=10485760
LOG_FILE_MAX_FILES=3

# Performance
COMPRESSION_LEVEL=3
CACHE_MAX_AGE=86400

# Monitoring
ENABLE_METRICS=false
METRICS_PORT=9090

# Health Check
HEALTH_CHECK_INTERVAL=30000
