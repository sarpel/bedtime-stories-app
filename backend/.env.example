# =============================================================================
# ÜRETİM ENV DOSYASI (Pi Zero 2 W) - MİNİMUM GEREKSİNİMLER
# =============================================================================
# Bu dosyayı backend/.env olarak kopyalayın ve API anahtarlarını doldurun.
# Aşağıdaki MODEL ve ENDPOINT değişkenleri ile birincil sağlayıcı anahtarları
# BOŞ BIRAKILIRSA backend ilgili endpoint'e istek geldiğinde 500 döner.
# Gereksiz tüm opsiyonel ayarlar kaldırıldı. Sade ve kilitli üretim yapı.
# =============================================================================

# -----------------------------------------------------------------------------
# OPENAI CONFIGURATION (Primary LLM Provider)
# -----------------------------------------------------------------------------
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5-mini
OPENAI_ENDPOINT=https://api.openai.com/v1/responses

# -----------------------------------------------------------------------------
# ELEVENLABS CONFIGURATION (Primary TTS Provider)
# -----------------------------------------------------------------------------
ELEVENLABS_API_KEY=
ELEVENLABS_MODEL=eleven_turbo_v2_5
ELEVENLABS_VOICE_ID=xsGHrtxT5AdDzYXTQT0d
ELEVENLABS_ENDPOINT=https://api.elevenlabs.io/v1/text-to-speech

# -----------------------------------------------------------------------------
# GEMINI LLM CONFIGURATION (Alternative LLM Provider)
# -----------------------------------------------------------------------------
GEMINI_LLM_API_KEY=
GEMINI_LLM_MODEL=gemini-2.5-flash-lite
GEMINI_LLM_ENDPOINT=https://generativelanguage.googleapis.com/v1beta/models

# -----------------------------------------------------------------------------
# GEMINI TTS CONFIGURATION (Alternative TTS Provider)
# -----------------------------------------------------------------------------
GEMINI_TTS_API_KEY=
GEMINI_TTS_MODEL=gemini-2.5-flash-preview-tts
GEMINI_TTS_VOICE_ID=Despina
GEMINI_TTS_ENDPOINT=https://generativelanguage.googleapis.com/v1beta/models

# -----------------------------------------------------------------------------
# SERVER CONFIGURATION
# -----------------------------------------------------------------------------
NODE_ENV=production
PORT=8080
HOST=0.0.0.0

# -----------------------------------------------------------------------------
# DATABASE CONFIGURATION
# -----------------------------------------------------------------------------
DATABASE_PATH=./database/stories.db

# -----------------------------------------------------------------------------
# MEDIA & STORAGE CONFIGURATION
# -----------------------------------------------------------------------------
MEDIA_DIR=/var/lib/storyapp/media

# -----------------------------------------------------------------------------
# RATE LIMITING & SECURITY
# -----------------------------------------------------------------------------
LOG_LEVEL=warn

# -----------------------------------------------------------------------------
# SESSION & SECURITY (Optional)
# -----------------------------------------------------------------------------
MAX_CONCURRENT_TTS=1

# =============================================================================
# DEPLOYMENT NOTES
# =============================================================================
#
# ZORUNLU ANAHTARLAR:
#  - OPENAI_API_KEY veya alternatif olarak GEMINI_LLM_API_KEY
#  - ELEVENLABS_API_KEY veya alternatif olarak GEMINI_TTS_API_KEY
#  - Belirtilen tüm *_MODEL ve *_ENDPOINT değerleri boş kalmamalı.
# Boş bırakırsanız ilgili API çağrısı 500 döner (sessiz fallback yok).
# =============================================================================
